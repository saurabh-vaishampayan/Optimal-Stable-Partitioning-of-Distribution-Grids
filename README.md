# Optimal-Stable-Partitioning-of-Distribution-Grids
This repository contains code for the optimal stable partitioning of distribution grids.  To run it, you either need Jupyter Notebook installed, or you can copy the code to .py files  Within the repository we provide environment.yml file which containts the list of libraries that were installed (conda)
